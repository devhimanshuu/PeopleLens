services:
  - type: web
    name: peoplelens-api
    env: node
    plan: free
    buildCommand: pnpm install --frozen-lockfile && pnpm --filter=@peoplelens/api prisma:generate && pnpm build --filter=@peoplelens/api
    startCommand: pnpm --filter=@peoplelens/api prisma:deploy && pnpm --filter=@peoplelens/api start:prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 20.19.0
      - key: TRUST_PROXY
        value: 'true'
      - key: DATABASE_URL
        sync: false
      - key: NEON_AUTH_BASE_URL
        sync: false
      - key: CORS_ORIGINS
        sync: false
      - key: SESSION_CACHE_TTL_MS
        value: '60000'
      - key: RATE_LIMIT_TTL_MS
        value: '60000'
      - key: RATE_LIMIT_MAX
        value: '120'
      - key: SWAGGER_ENABLED
        value: 'true'
      - key: SWAGGER_PATH
        value: docs
      - key: REQUEST_LOGGING_ENABLED
        value: 'true'
